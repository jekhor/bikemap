<div id="comment-notice"></div>
<%= form_for @comment, :remote => true do |f| %>
  <fieldset>
    <legend>Отправка комментария</legend>
  <p>
    <%= f.text_area :text, :rows => 2 %>
  </p>

  <p>
    <%= f.submit 'Отправить' %>
  </p>
</fieldset>
<% end %>
