feature = theCustomMap._selectedFeature;

feature.bindPopup("<%= escape_javascript(render :file => 'features/edit', :layout => 'layouts/feature-popup', :formats => [:html])%>");
feature.closePopup();
feature.openPopup();

$('input.cancel').click(function(e) {
  theMap.closePopup();
  return false;
});

$('.feature-del-link').click(function(e) {
  $.ajax({    url: '/features/' + feature.properties.id,
    type: 'DELETE',
    dataType: 'script',
  });
  return false;
});


$('a[rel*="lightbox"]').lightBox();

$('input[name="feature[existing]"]').click(function(e) {
  $('.field#photo').toggle($('#feature_existing_true')[0].checked);
});
